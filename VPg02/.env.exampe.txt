# Получены в Pinecone (https://www.pinecone.io/)
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_ENVIRONMENT=your-pinecone-environment   # например, "us-west1-gcp" (может быть не нужно в новых SDK)
PINECONE_INDEX_NAME=nemo

# Ключ от proxyapi.ru (или напрямую OpenAI, но через прокси)
OPENAI_API_KEY=your-proxyapi-key
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1

# Модель для эмбеддингов (размерность 1536)
EMBEDDING_MODEL=text-embedding-3-small

# Модель для генерации (если используется – мы не используем в ДЗ, но оставляем для справки)
# Ограничение: не выше gpt-3.5-turbo-16k
OPENAI_MODEL=gpt-3.5-turbo-16k